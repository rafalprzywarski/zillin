(ns zillin.core
    (import java.io.File)
    (import java.awt.image.BufferedImage)
    (import javax.imageio.ImageIO)
    (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
